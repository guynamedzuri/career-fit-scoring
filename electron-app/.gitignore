node_modules
dist
dist-installer
dist-electron
*.log
.DS_Store
.env
.env.local
# 빌드된 JavaScript 파일들 (TypeScript 컴파일 결과)
electron/main.js
electron/preload.js
electron/**/*.js
electron/**/*.js.map
# 소스 파일이 아닌 빌드 산출물
electron/src/
# TypeScript 컴파일 중간 디렉토리
electron/electron-app/
# 빌드 시 copy-poppler-for-build.js가 복사하는 poppler-windows (원본은 프로젝트 루트)
poppler-windows/
# 빌드 시 copy-scripts-for-build.js가 복사하는 Python 스크립트 (원본은 프로젝트 루트 scripts/)
py-scripts/
